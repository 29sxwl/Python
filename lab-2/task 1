# Імпортуємо модуль math, щоб використовувати функції sqrt і sin
import math

# Оголошуємо функцію expression, яка обчислює значення виразу
def expression(x):
    # Обчислюємо вираз: корінь квадратний з 2, поділено на 2, помножено на sin(1 / x), і додано 1
    z = (math.sqrt(2) / 2) * math.sin(1 / x) + 1
    return z

# Зчитуємо значення x від користувача
x = float(input("Введіть значення x: "))

# Виводимо результат обчислення виразу
print("Значення виразу z =", expression(x))

# Оголошуємо функцію check_nedostat, яка перевіряє, чи число є достатнім чи недостатнім
def check_nedostat(n):
    # Ініціалізуємо змінну sum для обчислення суми дільників
    sum = 0
    
    # Знаходимо всі дільники числа n і додаємо їх до суми
    for i in range(1, n + 1):
        if n % i == 0:
            sum += i
        
    # Порівнюємо суму з числом n і виводимо результат
    if sum < n:
        print("Число є недостатнім")
    else:
        print("Число є достатнім")

# Зчитуємо число n від користувача
n = int(input("Введіть число: "))

# Викликаємо функцію check_nedostat для перевірки числа на достатність
check_nedostat(n)
